# weblog
## local
```shell
hugo server
```
## build
```shell
hugo -D -d ./docs -b https://tsinling.org
```
## deploy
```
git add .
git commit -m "new post comit"
git push -u origin main
```
